# 原来这里第一行是 `**/*_test.go`，把整个仓库的 Go 测试文件全忽略掉了。
#
# 后果：仓库里长期只有一个测试文件（mtpay/internal/sign_test.go，而且是 git add -f
# 强制加进来的），任何人写了测试都会在提交时静默消失 —— 而 CI 跑 `go test ./...`
# 也就等于什么都没测。
#
# 这一行几乎肯定是误加的（Go 的测试文件本来就不会进编译产物，没有忽略它的理由），
# 所以这里去掉。这是本次改动里**唯一**动到的现有文件；它不影响任何调用方的编译。
help2pay/.DS_Store
.idea/
